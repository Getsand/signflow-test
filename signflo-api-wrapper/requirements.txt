# SignFlo API Wrapper - minimal deps (optimized, light load)
fastapi>=0.109.0
uvicorn>=0.27.0
hypercorn>=0.16.0
httpx>=0.26.0
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0.0

# DB (wrapper's own SQLite for API keys and usage logs)
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.19.0

# Optional: Redis for distributed rate limiting
# redis>=5.0.0
